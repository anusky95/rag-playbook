# ============================================================
# ULTIMATE RAG — Python Dependencies
# ============================================================
# Install everything:  pip install -r requirements.txt
# Python 3.9+

# ── LLM (Claude) ────────────────────────────────────────────
anthropic>=0.45.0          # Anthropic Python SDK (Claude)
python-dotenv>=1.0.0       # Load API keys from .env file

# ── Embeddings & ML ─────────────────────────────────────────
sentence-transformers>=3.0.0  # Embedding models (all-MiniLM-L6-v2 etc.)
scikit-learn>=1.5.0            # cosine_similarity, metrics
numpy>=1.26.0                  # Array math
transformers>=4.43.0           # HuggingFace models
tokenizers>=0.19.0             # Fast tokenization

# ── Vector Stores ────────────────────────────────────────────
chromadb>=0.5.0            # Local vector database
faiss-cpu>=1.7.4           # Facebook AI Similarity Search (use faiss-gpu if you have a GPU)

# ── Data & Evaluation ────────────────────────────────────────
datasets>=2.21.0           # HuggingFace datasets (BEIR, MS MARCO etc.)
nltk>=3.9.0                # Tokenization (word, sentence)

# ── Frameworks (Section 11) ──────────────────────────────────
langchain>=0.2.11
langchain-community>=0.2.10
langchain-anthropic>=0.1.0
llama-index>=0.10.0
llama-index-embeddings-huggingface>=0.2.0
llama-index-llms-anthropic>=0.3.0

# ── Deployment (Section 7) ───────────────────────────────────
fastapi>=0.112.0
uvicorn>=0.30.0

# ── Visualisation ────────────────────────────────────────────
matplotlib>=3.9.0
seaborn>=0.13.0
